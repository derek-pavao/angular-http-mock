describe('A sample test', function () {

    describe('An array', function () {


        it ('should count the number of things in it and expose it as length', function () {
            var arr = ['2','1'];

            arr.length.should.equal(2);
        });

    });

});
